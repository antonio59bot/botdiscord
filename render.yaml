services:
  - type: web
    name: botdiscord
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.11
      - key: DISCORD_TOKEN
        value: ton_token_ici
